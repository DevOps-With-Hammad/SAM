Django==5.0.6
asgiref==3.8.1
sqlparse==0.4.4
pytz==2024.1
python-dotenv==1.0.1  # For managing environment variables
gunicorn==20.1.0  # WSGI server (useful for deployment)
psycopg2-binary==2.9.6  # PostgreSQL adapter (if using PostgreSQL)
Pillow==10.3.0  # For image processing (if needed)
requests==2.31.0  # For making HTTP requests (if used in your project)
uvicorn==0.29.0  # ASGI server for running FastAPI or Django with ASGI
